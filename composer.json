{
  "name":"my-gallery",
  "description": "Wordpress gallery plugin.",
  "require-dev": {
    "phpunit/phpunit": "^7"
},
  "scripts": {
    "test": "vendor/bin/phpunit --coverage-clover Coverage.clover"
  }
}